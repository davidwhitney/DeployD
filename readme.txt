Hello DeployD

# Custom Actions

Put any custom action PowerShell scripts in the /customActions folder at the root of your Nuget package. After the package is installed they will be available to execute remotely through the agent.